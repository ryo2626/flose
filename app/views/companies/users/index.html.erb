<p id="notice"><%= notice %></p>

<h1>Companies Users</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @companies_users.each do |companies_user| %>
      <tr>
        <td><%= link_to 'Show', companies_user %></td>
        <td><%= link_to 'Edit', edit_companies_user_path(companies_user) %></td>
        <td><%= link_to 'Destroy', companies_user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Companies User', new_companies_user_path %>
