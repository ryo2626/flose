<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <%= link_to new_registration_path(resource_name), class: "btn btn-primary text-white mt-5 py-1 px-4" do %>
  <h3 class="font-weight-bolder">新規登録</h3>
  <% end %>
<% end %><br />
